# Vexmo Digital

Static site for Vexmo Digital.

## About
This repository contains the static site assets (HTML, CSS, JS, images) for Vexmo Digital.

## Local preview
- Open `index.html` in your browser, or run a simple HTTP server from the project root:

```bash
# Python 3
python -m http.server 8000

# Or using Node (if http-server installed)
npx http-server -p 8000
```

Then visit: http://localhost:8000

## Build / Tooling
If this project uses a build tool (e.g., webpack, parcel, vite), include build steps here. If not, the site is pure static and requires no build.

Example (if using npm):
```bash
npm install
npm run build
npm start
```

## Contributing
- Edit or add files under the repo root.
- Add meaningful commit messages.
- Open PRs against `main`.

## License
Specify a license (e.g., MIT) or remove this section if not applicable.

## Contact
For questions, reach out to the repo owner: @murthypatta
